import React from "react";

const PauseButton = ({toggleButton}) => {
  
  return (
    <>
      <div 
        id="pause-btn2"
        onClick={toggleButton}
      ></div>
    </>
  );
};

export default PauseButton;
